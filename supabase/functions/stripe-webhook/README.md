# Stripe webhook — DESCONTINUADO

Esta Edge Function foi substituída por [`../cakto-webhook/`](../cakto-webhook/index.ts),
já que o produto agora é vendido pela **Cakto**.

O código original em `index.ts` foi mantido apenas como referência histórica.
**Não faça deploy desta função.** Caso queira limpar o repositório, é seguro
remover este diretório inteiro.
